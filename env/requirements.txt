# pip-only install (if you prefer venv over conda).
# Recommended: Python 3.10.  Create a fresh env first, then:  pip install -r env/requirements.txt
#
# Note: on Apple Silicon replace `tensorflow` with `tensorflow-macos` (+ optionally `tensorflow-metal`).

numpy
pandas
scipy
scikit-learn
scikit-image>=0.22,<0.25
matplotlib
seaborn
h5py
tables
opencv-python
tifffile
imagecodecs
jupyterlab
ipywidgets

# single cell / spatial
scanpy
anndata
squidpy
celltypist
leidenalg
igraph

# bin2cell + StarDist
bin2cell
stardist
csbdeep
tensorflow

# TissueTag2 — installed from the main branch
git+https://github.com/DRPTSB/TissueTag2.git@main
panel
bokeh
jupyter-bokeh
holoviews
datashader
tinybrain
